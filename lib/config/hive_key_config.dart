class HiveKeyConfig {
  HiveKeyConfig._();
  static const tokenBoxKey = 'tokens';
  static const refreshTokenKey = 'refresh_token';
  static const accessTokenKey = 'access_token';
  static const user = 'user';
}
